import 'package:flutter/material.dart';
import 'package:omnicart/admin/admin_home.dart';

class AdHome extends StatelessWidget {
  const AdHome({super.key});

  @override
  Widget build(BuildContext context) {
    return AdminHome();
  }
}
